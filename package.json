{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "changes": "changeset",
    "publish-packages": "turbo run build && changeset version && pnpm install && pnpm publish -r --access public --filter !@react-av/docs --no-git-checks"
  },
  "author": "Daniel Wykerd <daniel@wykerd.dev>",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.25.0",
    "turbo": "^1.5.6"
  },
  "packageManager": "pnpm@10.12.1"
}
